[project]
name = "kernel-spec"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "cffi>=1.16,<3",
    "hypothesis>=6,<7",
    "marimo[mcp]>=0.19,<1",
    "pybindgen>=0.22,<0.23",
    "pydantic>=2,<3",
]

[tool.uv]
package = false
