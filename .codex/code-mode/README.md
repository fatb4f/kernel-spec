# Code-mode operator artifacts

The supervisory boundary admits constrained-client coordinates only from the
separate `run-bindings/`, `session-bindings/`, and `requests/` roots here.
Their contents are operator-issued runtime material and remain untracked.
